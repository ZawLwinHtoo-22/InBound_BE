spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=InBound_BE;encrypt=true;trustServerCertificate=true;
    username: sa
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect
        format_sql: true

